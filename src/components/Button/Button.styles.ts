import { StyleSheet } from "react-native";

export default StyleSheet.create({
  button: {
    backgroundColor: "#000",
    padding: 10,
  },
  buttonText: {
    color: "#fff",
    textAlign: "center",
  },
});
